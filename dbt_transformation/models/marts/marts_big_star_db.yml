version: 2

models:
  - name: dim_customers
    columns:
      - name: customer_id
        tests:
          - unique
      - name: (customer_id, email, city, country)
        tests: 
          - not_null